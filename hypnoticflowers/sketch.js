let x = [];
let y = [];
let pts = 400;
let r = 100;
let f_radius = [];
let f_amp = 20;
let period = 10;
let rotate = 0;

function setup() {
  createCanvas(400, 400);
  angleMode(DEGREES);
}

function draw() {
  background(220);
  translate(width / 2, height / 2);
}
